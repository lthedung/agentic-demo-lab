.runner_01_job:
  tags:
    - runner_01
  before_script:
    - echo "Branch=${CI_COMMIT_BRANCH:-none} Tag=${CI_COMMIT_TAG:-none} SHA=${CI_COMMIT_SHORT_SHA:-local}"

.artifact_1_week:
  artifacts:
    expire_in: 1 week
